node_modules/
client/dist/
client/public/data/
server/db/*.sqlite
server/db/*.sqlite-*
*.log
.DS_Store
# geometry-cache.json IS committed (deterministic, offline-safe seeding).
# client/public/data/ is generated by `npm run build:pages` from the DB and
# hosted via the gh-pages branch, so it is not committed to main.

{
  "name": "@allfish/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AllFish API server (Fastify + SQLite). Serves cited water-body + fish-species data and user reviews.",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "seed": "node db/seed.js",
    "test": "node test/api.test.js"
  },
  "dependencies": {
    "@fastify/cors": "^10.0.1",
    "@fastify/rate-limit": "^10.2.1",
    "@fastify/static": "^8.0.3",
    "better-sqlite3": "^11.8.1",
    "fastify": "^5.2.1"
  }
}
